require 'spec_helper'

describe Browsermob::Rest do
  it 'has a version number' do
    expect(Browsermob::Rest::VERSION).not_to be nil
  end

  # it "should have a VERSION of type string" do
  #   expect(dummy_class::VERSION).to be_a String
  # end

  # it "should have a VERSION of the format x.y.z" do
  #   expect(dummy_class::VERSION.match(/[0-9]+\.\d+\.\d+/)).to_not be_nil
  # end
  
end
